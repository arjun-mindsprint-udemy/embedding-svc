apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "embedding-svc.fullname" . }}-test-connection"
  labels:
    {{- include "embedding-svc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "embedding-svc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
