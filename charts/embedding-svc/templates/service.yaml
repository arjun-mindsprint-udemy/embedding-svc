apiVersion: v1
kind: Service
metadata:
  name: {{ include "embedding-svc.fullname" . }}
  labels:
    {{- include "embedding-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3022
      protocol: TCP
      name: http
  selector:
    {{- include "embedding-svc.selectorLabels" . | nindent 4 }}
